#include "main.h"

/**
 * _strlen - function that count lenght
 * description: count lenght of string
 * @s:string
 * Return: always 0
*/

int _strlen(char *s)
{
	int i;
	int strlen = 0;

	for (i = 0; i < sizeof(*s) - 1; i++)
	{
	strlen += 1;
	}
}
